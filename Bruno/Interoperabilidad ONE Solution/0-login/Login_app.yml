info:
  name: Login_app
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/login"
  body:
    type: json
    data: |-
      {
        "usuario": "admin",
        "password": "S1N3RG1@S"
      }
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
