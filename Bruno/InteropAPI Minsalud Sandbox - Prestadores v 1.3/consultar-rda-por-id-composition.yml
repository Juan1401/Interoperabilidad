info:
  name: consultar-rda-por-id-composition
  type: http
  seq: 14

http:
  method: GET
  url: "{{APIMurl}}/Composition/<reemplazar-por-id-composition>/$document"
  headers:
    - name: Ocp-Apim-Subscription-Key
      value: "{{APIMsubsKey}}"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
