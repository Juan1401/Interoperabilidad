info:
  name: terminologia-validate-code
  type: http
  seq: 17

http:
  method: GET
  url: "{{APIMurl}}/CodeSystem/IUM/$validate-code?code=1A1000101000100"
  headers:
    - name: Ocp-Apim-Subscription-Key
      value: "{{APIMsubsKey}}"
  params:
    - name: code
      value: 1A1000101000100
      type: query
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
