info:
  name: terminologia-consultar-codificacion
  type: http
  seq: 16

http:
  method: GET
  url: "{{APIMurl}}/CodeSystem/IUM"
  headers:
    - name: Ocp-Apim-Subscription-Key
      value: "{{APIMsubsKey}}"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
