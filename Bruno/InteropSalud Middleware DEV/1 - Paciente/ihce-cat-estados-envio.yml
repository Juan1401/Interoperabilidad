info:
  name: ihce-cat-estados-envio
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/hl7/system/ihce-cat-estados-envio"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
